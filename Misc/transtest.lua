component = require("component")
s = require("sides")
t = component.proxy(component.get("c49"))

function getAmountOfItem(name)
    count = 0

    for i in t.getAllStacks(s.right) do

    end

    return count
end